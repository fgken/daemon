{
  // https://go.microsoft.com/fwlink/?LinkId=733558
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build daemon (Debug)",
      "type": "shell",
      "command": "bazel build //src:main -c dbg",
      "osx": {
        "command": "bazel build //src:main -c dbg --spawn_strategy=standalone"
      }
    }
  ]
}